!!!
%html
%head
  %title Find Car Theft
  = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
  = javascript_include_tag 'application', 'data-turbolinks-track' => true
  = csrf_meta_tags

%body
  %nav.navbar.navbar-default
    .container
      .navbar-brand Find Cars Theft
      %ul.nav.navbar-nav
        %li= link_to "All cars", root_path
        - Category.all.each do |category|
          %li= link_to category.name, cars_path(category: category.name)
      = link_to "Add car", new_car_path, class: "navbar-text navbar-right navbar-link"
  .container

    %p
      = notice
    %p
      = alert   

    = yield
